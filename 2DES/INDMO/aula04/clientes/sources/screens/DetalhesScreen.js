import React from 'react'
import { StyleSheet, View, FlatList, Text, Image } from 'react-native-web'

export default function DetalhesScreen({navigation, rooter}) {
    console.log(route.params.dados);
    return (
        <View style={styles.container} >
            <Text>Detalhes</Text>
        </View>
    )
}
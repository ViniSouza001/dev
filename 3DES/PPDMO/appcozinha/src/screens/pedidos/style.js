import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    fundo: {
        width: '100vw',
        height: '100vh',
    },
    itemLista: {
        color: '#fff'
    },
    container: {
        position: "absolute"
    }
})

export default styles